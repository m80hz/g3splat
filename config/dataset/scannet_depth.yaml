defaults:
  - base_dataset
  - view_sampler: bounded

name: scannet_depth
roots: [datasets/scannetv1_test]
context_pair_file: scannet_test_pairs.txt
num_target_views: 4
make_baseline_1: false
augment: true

input_image_shape: [256, 256]
original_image_shape: [968, 1296]
cameras_are_circular: false

baseline_min: 1e-3
baseline_max: 1e2
max_fov: 120.0

skip_bad_shape: false
